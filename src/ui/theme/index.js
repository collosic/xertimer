import { createMuiTheme } from '@material-ui/core/styles';

const palette = {
  primary: { main: '#4A148C' },
  secondary: { main: '#616161', contrastText: '#ffffff' },
};
const themeName = 'Persian Indigo Dove Gray Bird';

export default createMuiTheme({ palette, themeName });
